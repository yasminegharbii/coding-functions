# Hands-on Coding using Functions — Part 1

## Exercices

### Exigences de base

---

## myFirstFunction

Écris une fonction appelée **`myFirstFunction`** qui ne prend aucun paramètre et ne fait rien, puis invoque la fonction.

```javascript
function myFirstFunction() {
  // Write your code here
}

myFirstFunction();
```

---

##  helloWorld

Écris une fonction appelée **`helloWorld`** qui affiche **"Hello World"** dans la console et invoque-la.

 *Indice : utilise `console.log()`.*

```javascript
function helloWorld() {
  // Write your code here
}

helloWorld(); // -> "Hello World"
```

---

##  returnFive

Écris une fonction appelée **`returnFive`** qui ne prend aucun paramètre et retourne le nombre **5** lorsqu’elle est invoquée.

```javascript
function returnFive() {
  // Write your code here
}

returnFive(); // -> 5
```

---

## helloWorldAgain

Écris une fonction appelée **`helloWorldAgain`** qui ne prend aucun paramètre et retourne la chaîne **"Hello World Again"**.

```javascript
function helloWorldAgain() {
  // Write your code here
}

helloWorldAgain(); // -> "Hello World Again"
```

---

##  returnParameter

Écris une fonction appelée **`returnParameter`** qui prend un paramètre et retourne ce paramètre.

```javascript
function returnParameter(parameter) {
  // Write your code here
}

returnParameter(1);    // -> 1
returnParameter(2);    // -> 2
returnParameter("Hi"); // -> "Hi"
```

---

##  add

Écris une fonction appelée **`add`** qui prend deux nombres comme paramètres et retourne leur somme.

```javascript
function add(firstNumber, secondNumber) {
  // Write your code here
}

add(1, 1); // -> 2
add(2, 2); // -> 4
```

---

##  subtract

Écris une fonction appelée **`subtract`** qui prend deux nombres comme paramètres et retourne leur différence.

```javascript
function subtract(firstNumber, secondNumber) {
  // Write your code here
}

subtract(2, 1); // -> 1
subtract(3, 3); // -> 0
subtract(1, 2); // -> -1
```

---

##  multiply

Écris une fonction appelée **`multiply`** qui prend deux nombres comme paramètres et retourne leur produit.

```javascript
function multiply(firstNumber, secondNumber) {
  // Write your code here
}

multiply(2, 3); // -> 6
multiply(4, 5); // -> 20
```

---

##  stringLength

Écris une fonction appelée **`stringLength`** qui retourne la longueur d’une chaîne.

*Indice : utilise `.length`.*

```javascript
function stringLength(string) {
  // Write your code here
}

stringLength("Hello"); // -> 5
stringLength("Hi");    // -> 2
```

---

## concatTwoStrings

Écris une fonction appelée **`concatTwoStrings`** qui concatène deux chaînes.

```javascript
function concatTwoStrings(firstString, secondString) {
  // Write your code here
}

concatTwoStrings("Hello", "World"); // -> "HelloWorld"
concatTwoStrings("Cute", "Kitty");  // -> "CuteKitty"
```

---


